/**
 * Shared custom forgot-password page (all panels).
 *
 * How to use:
 *   1. Copy this file to: ForgotPassword.jsx  (remove .sample)
 *   2. Rebuild / restart Vite
 *
 * Priority:
 *   1. dynamic-public-routes/{prefix}/ForgotPassword.jsx
 *   2. dynamic-public-routes/ForgotPassword.jsx
 *   3. pages/ForgotPassword.jsx
 */

import React from "react";
import DefaultForgotPassword from "../pages/ForgotPassword";

function ForgotPassword() {
  return (
    <div className="custom-forgot-password">
      <DefaultForgotPassword />
    </div>
  );
}

export default ForgotPassword;
